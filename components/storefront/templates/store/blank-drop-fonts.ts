/**
 * Blank drop — Geist Mono for a utilitarian catalogue lettering system.
 * Craft bar: yeezy.com (IBM Plex Mono grammar) without runtime Google Fonts.
 */
import { geistMono } from "@/app/fonts/geist-mono";

export const blankDropMono = geistMono;

export const blankDropFontVariables = blankDropMono.variable;
