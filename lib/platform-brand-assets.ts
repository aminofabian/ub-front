/**
 * Canonical platform brand image paths (files in `public/`).
 * Host logo = full lockup; favicon + app icon share the same mark tile.
 */

export const PLATFORM_HOST_LOGO_SRC = "/kiosklogoo.png";
export const PLATFORM_APP_ICON_SRC = "/app-icon.png";
/** Browser tab favicon — same artwork as the app icon. */
export const PLATFORM_FAVICON_SRC = PLATFORM_APP_ICON_SRC;

/** Intrinsic aspect of the host lockup PNG (1536×1024). */
export const PLATFORM_HOST_LOGO_ASPECT = 1536 / 1024;
